我想在当前目录`~/workarea/code/chrome-copilot`构建一个 Chrome 浏览器 plugin 名为`Chrome Copilot`。

这个 plugin 需要实现如下功能：
对于 Chrome 浏览器中选中的文本，右键点击弹出的列表中出现`Chrome Copilot`选项，选择该选项后，Chrome Copilot 会读取选中的文本，并使用用户配置的 LLM 模型和 Prompt 对这段文本进行解释。


